## 1. 实施
- [x] 1.1 新增按 `userid` 的用户缓存 key 和统一读取接口
- [x] 1.2 扩展用户缓存写入与失效逻辑，覆盖 `deviceid` 和 `userid`
- [x] 1.3 将登录链路切换为通过 `UserService` 读取用户
- [x] 1.4 为 `user.userid` 增加模型索引和迁移

## 2. 验证
- [x] 2.1 补充单元测试，覆盖缓存命中、缓存未命中、Redis 降级和用户不存在场景
- [ ] 2.2 在本地 MySQL 环境执行迁移并验证索引生效
